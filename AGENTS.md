## Project Directive

- This project is developed remotely. When sharing app or service URLs with the user, do not use `localhost` or `127.0.0.1`; use the current server IP address plus the port instead, for example `http://150.65.181.206:<port>/`.
